{
  "name": "devatlas-functions",
  "version": "1.0.0",
  "description": "DevAtlas API — read-only Cloud Function for Firebase Hosting (Spark plan compatible, no secrets)",
  "main": "src/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,hosting",
    "deploy": "firebase deploy --only functions,firestore,hosting"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^4.9.0"
  },
  "private": true
}
